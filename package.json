{
  "name": "forestvol",
  "version": "5.1.0",
  "description": "ForestVol MVP — build tooling para el frontend Vue.js",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "note": "Los tests del harness usan pytest (ver tests/harness/). El frontend usa frontend/package.json."
}
